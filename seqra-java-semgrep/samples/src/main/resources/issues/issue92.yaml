rules:
  - id: i92
    message: "message"
    languages: [java]
    severity: WARNING
    mode: taint
    pattern-sources:
      - patterns:
          - pattern: (Creator $CR)
    pattern-sinks:
      - patterns:
          - pattern: |
              (Searcher $IDC).search(...)

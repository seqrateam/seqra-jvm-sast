rules:
  - id: taint-RuleNoFocus
    languages:
      - java
    severity: ERROR
    message: match taint/RuleNoFocus
    mode: taint
    pattern-sources:
      - patterns:
          - pattern: $X = src();
    pattern-sinks:
      - patterns:
          - pattern: sink($Y);
